const Form = () => {
    const handleSubmit = () => {};
    
    return (
      <form onSubmit={handleSubmit}>
          <input type='text' /> 
          <button type='submit'>Submit</button>
      </form>
    );
};

export default Form;